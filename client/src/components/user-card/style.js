import styled from 'styled-components';

const UserCardWrapper = styled.div`
	.desc {
		color: #8a919f;
		font-size: 14px;
		margin-top: 10px;
	}
	.username {
		font-weight: 500;
		position: relative;
		left: 15px;
		font-size: 24px;
	}
`;
export default UserCardWrapper;
